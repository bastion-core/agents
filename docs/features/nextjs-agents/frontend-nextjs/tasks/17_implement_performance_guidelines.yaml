# tasks/17_implement_performance_guidelines.yaml
task: implement_performance_guidelines
phase: 9 - Cross-cutting Concerns
level: L1
parent: technical.yaml
status: PENDING
depends_on: [12_implement_component_pattern]
assigned_subagent: nextjs-development:frontend-nextjs

scope: |
  Agregar la seccion de Performance Guidelines al agente frontend-nextjs.md.
  Cubrir la seccion performance del technical.yaml:

  1. Memoization:
     - useMemo: Solo para computaciones costosas (filter/sort de arrays grandes)
     - useCallback: Solo cuando se pasa handler como prop a componente memoizado
     - React.memo: Solo para componentes que re-renderizan frecuentemente sin cambios
     - NO memoizar por defecto. Medir primero.

  2. Code Splitting:
     - Next.js App Router hace code splitting automatico por ruta
     - dynamic() import para componentes pesados (maps, charts)
     - Lazy load modals y sidebars que no son visibles inicialmente

  3. Data Fetching:
     - Paginacion server-side para listas grandes
     - No cargar todos los datos de una vez
     - Debounce en campos de busqueda (300ms)
     - Cancelar requests anteriores al cambiar filtros

  4. Images:
     - Usar next/image para optimizacion automatica
     - Definir width y height para evitar layout shifts
     - Lazy loading por defecto (loading='lazy')

  Enfatizar que NO se debe memoizar por defecto -- medir primero.

files_to_modify:
  - plugins/nextjs-development/agents/frontend-nextjs.md

patterns:
  - memoization_guidelines: memoizar solo cuando hay problema medible
  - code_splitting: dynamic() para componentes pesados
  - server_side_pagination: paginacion server-side para listas grandes
  - debounce: 300ms para campos de busqueda
  - next_image: next/image para optimizacion de imagenes

acceptance:
  - prompt incluye 4 categorias de performance con reglas claras
  - prompt indica NO memoizar por defecto (medir primero)
  - prompt indica dynamic() para componentes pesados
  - prompt indica paginacion server-side y debounce 300ms
  - prompt indica next/image con width/height y lazy loading

context_files:
  - docs/features/nextjs-agents/frontend-nextjs/technical.yaml  # performance section
