# tasks/06_implement_bloc_and_widget_quality_checks.yaml
task: implement_bloc_and_widget_quality_checks
phase: 4 - Code Quality Review
level: L3
parent: technical.yaml
status: PENDING
depends_on: [05_implement_dart_and_freezed_quality_checks]
assigned_subagent: flutter-development:reviewer-mobile-flutter

scope: |
  Agregar al agente reviewer-mobile-flutter.md la segunda parte de Step 3: Code Quality Review,
  cubriendo las verificaciones de BLoC patterns y Widget patterns.
  Cubrir las secciones del technical.yaml:
  - pipeline.step_3_code_quality_review (bloc_checks, widget_checks)

  Contenido a incluir:

  1. BLoC Checks:
     - event_map_delegation: BLoC usa event.map() para delegar a handlers privados
     - private_handlers: handlers como _onEventName(emit) con prefijo underscore
     - emit_loading_first: emitir estado loading antes de operaciones async
     - result_when_in_handlers: result.when() para manejar success/failure en handlers
     - constructor_injection: dependencias inyectadas via constructor (no get_it directo)
     - helper_extraction: extraer helpers cuando BLoC tiene >6 event handlers o >200 lineas
     - no_abstract_base_bloc: NO crear clases BLoC base abstractas
     - factory_registration: BLoCs registrados como registerFactory en DI (nueva instancia por widget)

  2. Widget Checks:
     - bloc_builder_for_rebuild: BlocBuilder para reconstruir widgets segun estado
     - bloc_consumer_for_side_effects: BlocConsumer para side effects + rebuild
     - bloc_listener_for_navigation: BlocListener para side effects puros (navegacion, snackbar)
     - state_when_exhaustive: state.when() para cubrir todos los estados exhaustivamente
     - no_business_logic: widgets NO contienen logica de negocio
     - voltop_components: usar VoltopButton, VoltopText, VoltopTextField en lugar de widgets raw
     - voltop_colors: usar VoltopColors en lugar de valores Color() crudos
     - phosphor_icons: usar PhosphorIcons para iconografia
     - screen_naming: screens nombrados como {name}_screen.dart
     - widget_naming: widgets nombrados como {name}_widget.dart o descriptivamente

  Incluir ejemplos buenos y malos para cada check, especialmente para:
  - event.map() delegation pattern (como se ve un BLoC correcto)
  - BlocBuilder vs BlocConsumer vs BlocListener (cuando usar cada uno)
  - VoltopUI vs raw Flutter widgets

files_to_modify:
  - plugins/flutter-development/agents/reviewer-mobile-flutter.md

patterns:
  - event_map_delegation: BLoC constructor con event.map() delegando a handlers privados
  - emit_loading_first: siempre emitir loading antes de operacion async
  - bloc_widget_selection: BlocBuilder/BlocConsumer/BlocListener segun caso de uso
  - voltop_ui: componentes VoltopUI obligatorios en lugar de Flutter raw widgets
  - helper_extraction: criterios para extraer BLoC handlers a helper classes

acceptance:
  - prompt incluye las 8 reglas de bloc_checks con ejemplos buenos/malos
  - prompt incluye las 10 reglas de widget_checks
  - prompt muestra ejemplo de event.map() delegation correcto
  - prompt diferencia BlocBuilder, BlocConsumer y BlocListener con cuando usar cada uno
  - prompt incluye regla de VoltopUI components obligatorios (VoltopButton, VoltopText, etc.)
  - prompt incluye regla de VoltopColors obligatorios (no Color() crudos)
  - prompt incluye criterios de helper extraction (>6 handlers o >200 lineas)
  - prompt incluye regla de factory_registration para BLoCs en DI

context_files:
  - docs/features/flutter-agents/reviewer-mobile-flutter/technical.yaml  # step_3 bloc_checks, widget_checks
