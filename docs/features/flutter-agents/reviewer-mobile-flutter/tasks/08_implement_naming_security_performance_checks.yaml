# tasks/08_implement_naming_security_performance_checks.yaml
task: implement_naming_security_performance_checks
phase: 4 - Code Quality Review
level: L2
parent: technical.yaml
status: PENDING
depends_on: [06_implement_bloc_and_widget_quality_checks]
assigned_subagent: flutter-development:reviewer-mobile-flutter

scope: |
  Agregar al agente reviewer-mobile-flutter.md la cuarta parte de Step 3: Code Quality Review,
  cubriendo naming conventions, security checks, performance checks y code smells.
  Cubrir las secciones del technical.yaml:
  - pipeline.step_3_code_quality_review (naming_conventions, security_checks, performance_checks, code_smells)

  Contenido a incluir:

  1. Naming Conventions - Files:
     - bloc: {feature}_bloc.dart
     - event: {feature}_event.dart
     - state: {feature}_state.dart
     - helper: {helper_name}_helper.dart
     - repository_interface: {feature}_repository.dart
     - repository_impl: {feature}_repository_impl.dart
     - api_provider: {feature}_api_provider.dart
     - service_interface: {feature}_service.dart
     - service_impl: {feature}_service_impl.dart
     - dto: {name}_dto.dart o {name}_request_dto.dart / {name}_response_dto.dart
     - screen: {screen_name}_screen.dart
     - widget: {widget_name}_widget.dart
     - test: {source_file}_test.dart
     - mock: {feature}_mock.dart

  2. Naming Conventions - Variables:
     - classes: PascalCase
     - methods: camelCase
     - variables: camelCase
     - constants: camelCase (Dart convention) o UPPER_SNAKE_CASE para compile-time
     - private_members: prefijo _ (underscore)
     - file_names: snake_case
     - enum_values: camelCase

  3. Security Checks:
     - no_hardcoded_secrets: no API keys, tokens o passwords en codigo
     - no_sensitive_data_in_logs: no log de passwords, tokens o datos sensibles
     - input_validation: validacion de inputs del usuario antes de enviar a API
     - secure_storage_for_tokens: tokens almacenados de forma segura
     - no_debug_in_production: no kDebugMode checks que expongan datos en release
     - env_variables: secretos en .env file, no en codigo

  4. Performance Checks:
     - const_constructors: usar const constructors cuando sea posible
     - avoid_rebuild: minimizar rebuilds innecesarios con BlocBuilder buildWhen
     - lazy_loading: carga lazy de pantallas pesadas
     - list_builder: ListView.builder para listas largas (no ListView con children)
     - image_caching: caching de imagenes de red
     - dispose_resources: dispose de controllers, streams, timers en dispose()
     - no_sync_heavy_ops: no operaciones pesadas en el hilo principal

  5. Code Smells:
     - god_widget: >300 lineas (extraer sub-widgets)
     - god_bloc: >200 lineas o >6 handlers sin helpers (extraer helpers)
     - magic_strings: usar constantes o enums
     - hardcoded_text: usar sistema i18n (.i18n)
     - direct_dio_in_bloc: usar repository -> API provider
     - business_logic_in_widget: extraer a BLoC
     - commented_out_code: codigo comentado sin proposito
     - excessive_nesting: >4 niveles (extraer widgets o metodos)
     - unused_imports: imports no utilizados
     - print_statements: usar CombinedLogger en lugar de print()

files_to_modify:
  - plugins/flutter-development/agents/reviewer-mobile-flutter.md

patterns:
  - naming_conventions: reglas de naming para archivos y variables Dart/Flutter
  - security_checks: 6 verificaciones de seguridad
  - performance_checks: 7 verificaciones de rendimiento Flutter
  - code_smells: 10 code smells comunes en Flutter

acceptance:
  - prompt incluye tabla o lista completa de naming conventions para archivos (14 tipos)
  - prompt incluye tabla o lista de naming conventions para variables (7 tipos)
  - prompt incluye las 6 reglas de seguridad
  - prompt incluye las 7 reglas de rendimiento
  - prompt incluye los 10 code smells con umbrales y acciones correctivas
  - prompt menciona CombinedLogger como alternativa a print()
  - prompt menciona sistema i18n (.i18n) para texto visible

context_files:
  - docs/features/flutter-agents/reviewer-mobile-flutter/technical.yaml  # step_3 naming/security/performance/code_smells
  - plugins/python-development/agents/reviewer-backend-py.md  # Security, Performance sections reference
