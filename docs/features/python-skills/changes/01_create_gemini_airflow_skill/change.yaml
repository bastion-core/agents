# change.yaml
change_id: v1.1-create-gemini-airflow-skill
feature: python_skills
title: Crear nueva SKILL de Gemini para desarrollo de DAGs de Airflow
status: planned

scope:
  description: |
    Crear una nueva SKILL llamada `airflow-dags-py` para Gemini.
    El objetivo es proporcionar a Gemini el conocimiento experto sobre la arquitectura y patrones de desarrollo de DAGs de Airflow para que pueda generar especificaciones técnicas precisas.
  in_scope:
    - "Crear el directorio de la skill en `gemini/spec-generator/.gemini/skills/airflow-dags-py/`."
    - "Crear el archivo `SKILL.md` dentro del directorio de la skill."
    - "Crear el directorio `references/` dentro de la skill."
    - "Copiar `architecture.md`, `dev_patterns.md`, y `state_management.md` de `context/airflow-python-dags/` al nuevo directorio `references/`."
  out_of_scope:
    - "La implementación de los agentes que consumirán esta skill."
    - "La modificación de los archivos de contexto originales en `context/airflow-python-dags/`."

acceptance_criteria:
  - "El archivo `change.yaml` es creado exitosamente en la ruta `docs/features/python-skills/changes/01_create_gemini_airflow_skill/change.yaml`."
  - "El directorio `gemini/spec-generator/.gemini/skills/airflow-dags-py/` es creado."
  - "El archivo `SKILL.md` se encuentra dentro del directorio de la nueva skill."
  - "El directorio `references/` se encuentra dentro del directorio de la nueva skill."
  - "Los archivos `architecture.md`, `dev_patterns.md` y `state_management.md` existen dentro del directorio `references/`."

affected_repos:
  - agents

metadata:
  created_by: product_owner
  created_at: "2024-07-30"
  target_date: "2024-08-06"
  priority: medium
